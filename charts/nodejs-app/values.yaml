replicaCount: 2

image:
  repository: lokesh0423acr.azurecr.io/nodejs-cicd-pipeline
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  targetPort: 3000

resources:
  limits:
    cpu: 500m
    memory: 128Mi
  requests:
    cpu: 250m
    memory: 64Mi

namespace: default
environment: default
